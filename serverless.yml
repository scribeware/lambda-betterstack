# For full config options, check the docs:
#    docs.serverless.com

service: lambda-betterstack

provider:
  name: aws
  runtime: nodejs20.x
  region: us-west-2
  environment:
    LOGTAIL_SOURCE_TOKEN: ${ssm:/prod/logtail-source-token}
    LOGTAIL_ENDPOINT: ${ssm:/prod/logtail-endpoint}

functions:
  forwarder:
    handler: handler.log

resources:
  Resources:
    ForwarderLambdaPermission:
      Type: 'AWS::Lambda::Permission'
      Properties:
        Action: 'lambda:InvokeFunction'
        FunctionName:
          Fn::GetAtt: [ForwarderLambdaFunction, Arn]
        Principal: logs.${self:provider.region}.amazonaws.com
  Outputs:
    ForwarderLambdaArn:
      Value:
        Fn::GetAtt: [ForwarderLambdaFunction, Arn]
